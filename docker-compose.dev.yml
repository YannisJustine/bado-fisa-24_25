services:
  app:
    build:
      context: ./src
    container_name: apache
    volumes:
      - ./src:/var/www/html
    ports:
      - "8080:8000"
    command: /bin/sh -c "php artisan serve --host 0.0.0.0"